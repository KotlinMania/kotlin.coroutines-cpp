/**
 * Transliterated from: kotlinx-coroutines-core/common/src/selects/SelectUnbiased.kt
 *
 * The upstream Kotlin file ports cleanly into the header alone, so this translation unit
 * exists only to ensure the build system has a compile target for the file pair.
 */
#include "kotlinx/coroutines/selects/SelectUnbiased.hpp"
