/**
 * Transliterated from: kotlinx-coroutines-core/common/src/internal/MainDispatcherFactory.kt
 *
 * The full interface lives in the matching header; the upstream Kotlin file declares
 * only an interface, so the source side carries no implementation.
 */
#include "kotlinx/coroutines/internal/MainDispatcherFactory.hpp"
