#include "kotlinx/coroutines/CoroutineExceptionHandler.hpp"

// NOTE:
// The helper `handle_coroutine_exception(...)` is provided as an inline function
// in `include/kotlinx/coroutines/CoroutineExceptionHandler.hpp`.
// This TU remains only to mirror the Kotlin source layout; no out-of-line
// definition is emitted here to avoid ODR violations.
